{
  "_comment": "Copy this file to config/secrets.json and fill in real values. secrets.json is gitignored and volume-mounted at /app/config/secrets.json — it overrides the server credentials in stations.json.",
  "admin_password": "CHANGE_ME",
  "ftp_user": "radio",
  "ftp_password": "CHANGE_ME"
}